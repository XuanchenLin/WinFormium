(() => {
    window.addEventListener("load", () => {
        // const page = document.body;
        // const themes = ["green", "blue", "red", "yellow"];
        // const index = Math.floor(Math.random() * themes.length);
        // page.classList.add(`${themes[index]}-theme`);
    });
})();
